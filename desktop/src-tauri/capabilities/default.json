{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal capability for the local desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
